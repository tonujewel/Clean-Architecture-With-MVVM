import 'package:flutter/material.dart';

class ForgetOtpScreen extends StatelessWidget {
  const ForgetOtpScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("forgot otp"),
      ),
    );
  }
}
